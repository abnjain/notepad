<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notepad | abnjain</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/styles/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-800 text-white">
    <div class="flex flex-col items-center justify-center h-screen">
        <h1 class="text-3xl font-bold mb-4">Notepad</h1>
        <form action="/save" method="post" class="m-4 text-center">
            <input type="text" name="title" placeholder="Enter Title" class="bg-zinc-800 block w-full m-auto mb-3 text-2xl text-white placeholder-gray-500 text-center" />
            <textarea type="textarea" name="details" placeholder="Enter Details" class="resize-none bg-zinc-800 block w-full m-auto mb-3 text-2xl text-white placeholder-gray-500 text-center"></textarea>
            <!-- <input type="submit" class="bg-blue-700 text-white font-bold mt-2 py-1 px-2 rounded" value="Save Note"> -->
            <button type="submit" class="bg-blue-700 text-xl text-white font-bold mt-2 py-1 px-2 rounded">Save Note</button>
        </form>
        <div class="flex flex-wrap gap-3 mt-20">
            <div class="flex flex-wrap gap-3">
                <% if(files.length > 0) { %> 
                    <% files.forEach(function(val) { %>
                        <div class="rounded-md px-6 py-3 bg-zinc-900">
                            <h1 class="text-4xl"><%= val %></h1>
                            <div class="flex flex-wrap gap-5">
                                <a class="text-blue-500 mt-5" href="/public/files/<%= val %>">Read More</a>
                                <a class="text-zinc-400 mt-5" href="/edit/<%= val %>">Edit File</a>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <h3 class="text-zinc-400">No Tasks Yet</h3>
                <% } %>
            </div>
        </div>
        
        </div>
    </div>

    <script src="scripts/script.js"></script>
</body>

</html>